{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "app",
  "description": "enables the app permissions",
  "windows": ["main"],
  "webviews": ["app"],
  "permissions": ["core:default", "log:default", "app:default"]
}
